#!/bin/bash

. "$(dirname "$0")"/../helpers/init.sh
helper mt_split2mono

usage() { echo "usage: $(print_cmdname) <sha1>"; }

[ "$#" -ge 1 ] || error "missing <sha1>"
[ "$#" -eq 1 ] || error "too many positional args"

mt_split2mono_init &&
mt_split2mono "$@"
